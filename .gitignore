# ── 数据（430 GB 原始数据，不上传） ──
data/

# ── 中间结果（不上传） ──
results/intermediate/
results/tables/

# ── Python 缓存 ──
__pycache__/
*.pyc

# ── 早期翻译脚本与文档（非复现代码） ──
translate_to_word.py
*.docx
*.doc
# 排除根目录 PDF（论文原文/翻译），保留 results/figures/ 下的出图
/*.pdf

# ── 中间结果、表格、日志（可重跑，不进 Git） ──
results/intermediate/
results/tables/
logs/

# ── 数据（~430 GB，不进 Git） ──
data/

# ── Python 缓存 ──
__pycache__/
*.pyc

# ── Python 代码备份压缩包（不入库） ──
*.zip

# ── 系统文件 ──
.DS_Store
Thumbs.db
